CREATE TABLE "public"."users_fcm_token" ("id" serial NOT NULL, "user_id" uuid NOT NULL, "fcm_token" text NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "auth"."users"("id") ON UPDATE cascade ON DELETE cascade);
