CREATE TABLE "public"."attendance" ("id" bigserial NOT NULL, "user_id" uuid NOT NULL, "photo_file_id" uuid NOT NULL, "attendance_type" text NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "auth"."users"("id") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("photo_file_id") REFERENCES "storage"."files"("id") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("attendance_type") REFERENCES "public"."attendance_type_enum"("attendance_type") ON UPDATE restrict ON DELETE restrict);
