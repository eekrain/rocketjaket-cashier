table:
  name: users_metadata
  schema: public
object_relationships:
- name: user
  using:
    foreign_key_constraint_on: user_id
select_permissions:
- permission:
    columns:
    - id
    - store_id
    - user_id
    filter:
      user_id:
        _eq: X-Hasura-User-Id
  role: me
