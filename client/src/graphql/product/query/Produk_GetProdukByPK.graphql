query Produk_GetProdukByPK($id: uuid!) {
  products_by_pk(id: $id) {
    id
    name
    photo_id
    selling_price
    discount
    capital_price
    product_category_id
    created_at
    updated_at
    product_category {
      name
    }
  }
}
