query Store_GetStoreByPK($id: Int!) {
  stores_by_pk(id: $id) {
    id
    name
    latitude
    longitude
    address
    created_at
    updated_at
    working_hour_end
    working_hour_start
    is_record_attendance
  }
}
